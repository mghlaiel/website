apiVersion: apps/v1
kind: Deployment
metadata:
  name: creometry-consulting-deploy
  labels:
    app: creometry-consulting-site
spec:
  replicas: 1
  selector:
    matchLabels:
      app: creometry-consulting-site
  template:
    metadata:
      labels:
        app: creometry-consulting-site
        community: creometry
    spec:
      containers:
      - name: creometry-consulting-site
        image: creometry/consulting-static:v0.0.3
        ports:
        - containerPort: 80
        resources:
          limits:
            memory: "128Mi"
            cpu: "250m"
          
---
apiVersion: v1
kind: Service
metadata:
  name: creometry-consulting-svc
spec:
  selector:
    app: creometry-consulting-site
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: creometry-consulting-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/rewrite-target: /
    # nginx.ingress.kubernetes.io/affinity-canary-behavior: "sticky"
    # nginx.ingress.kubernetes.io/affinity: cookie
   # nginx.ingress.kubernetes.io/enable-cors: "true"
    #nginx.ingress.kubernetes.io/cors-allow-methods: "GET"
   # nginx.ingress.kubernetes.io/server-alias: "www.creometry.com"
    nginx.ingress.kubernetes.io/from-to-www-redirect: "true"
spec:
  tls:
    - hosts:
        - "consulting.creometry.com"
      secretName: creometry-consulting-ingress-secret
       
  rules:
  - host: "consulting.creometry.com"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: creometry-consulting-svc
            port:
              number: 80
